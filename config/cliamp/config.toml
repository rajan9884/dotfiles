# cliamp — retro terminal music player (Winamp for the TTY)
# "Bind ~/Music to cliamp": the file browser and directory playback open here.
# Each folder under ~/Music is exposed as a playlist (generated by install.sh
# into playlists/<folder>.toml with a live [[dir]] source).
initial_directory = "~/Music"
theme = ""